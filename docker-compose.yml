services:
  # ═══════════════════════════════════════
  # DATABASE
  # ═══════════════════════════════════════
  database:
    image: postgres:16-alpine
    container_name: croo-database
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-croo}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-croo}
      POSTGRES_DB: ${DB_NAME:-croo_digital_experience}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-croo} -d ${DB_NAME:-croo_digital_experience}"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ═══════════════════════════════════════
  # BACKEND API (monolithe — legacy)
  # ═══════════════════════════════════════
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: croo-api
    restart: unless-stopped
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      DEBUG: ${DEBUG:-true}
      API_PORT: ${API_PORT:-4500}
      DATABASE_URL: postgresql://${DB_USER:-croo}:${DB_PASSWORD:-croo}@database:5432/${DB_NAME:-croo_digital_experience}
      SECRET_KEY: ${SECRET_KEY}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-30}
      REFRESH_TOKEN_EXPIRE_DAYS: ${REFRESH_TOKEN_EXPIRE_DAYS:-7}
      CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost:4200","http://localhost:4511","http://localhost:4700"]}
      RATE_LIMIT_MAX_ATTEMPTS: ${RATE_LIMIT_MAX_ATTEMPTS:-5}
      RATE_LIMIT_WINDOW_MINUTES: ${RATE_LIMIT_WINDOW_MINUTES:-15}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@croo.digital}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      ADMIN_FIRST_NAME: ${ADMIN_FIRST_NAME:-Admin}
      ADMIN_LAST_NAME: ${ADMIN_LAST_NAME:-Croo}
      GROQ_API_KEY: ${GROQ_API_KEY}
      HUNTER_API_KEY: ${HUNTER_API_KEY}
      SERPER_API_KEY: ${SERPER_API_KEY}
      WEBHOOK_API_KEY: ${WEBHOOK_API_KEY}
      DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
      RICK_REF_AUDIO_PATH: ${RICK_REF_AUDIO_PATH:-/app/app/voice/rick_ref.wav}
      MS365_CLIENT_ID: ${MS365_CLIENT_ID}
      MS365_CLIENT_SECRET: ${MS365_CLIENT_SECRET}
      MS365_TENANT_ID: ${MS365_TENANT_ID}
      MS365_REDIRECT_URI: ${MS365_REDIRECT_URI}
      MS365_WEBHOOK_HOST: ${MS365_WEBHOOK_HOST}
    ports:
      - "${API_PORT:-4500}:${API_PORT:-4500}"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${API_PORT:-4500}/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    depends_on:
      database:
        condition: service_healthy

  # ═══════════════════════════════════════
  # B4F API (Backend-for-Frontend — gateway)
  # ═══════════════════════════════════════
  b4f-api:
    build:
      context: ./apis/b4f-api
      dockerfile: Dockerfile
    container_name: croo-b4f-api
    restart: unless-stopped
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      DEBUG: ${DEBUG:-true}
      PORT: ${B4F_API_PORT:-8000}
      JWT_SECRET_KEY: ${SECRET_KEY}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost:4200","http://localhost:4511","http://localhost:4700"]}
      AUTH_API_URL: http://auth-api:${AUTH_API_PORT:-8001}
      CRM_API_URL: http://crm-backend-api:${CRM_API_PORT:-8002}
      AI_AGENT_API_URL: http://ai-agent-api:${AI_AGENT_API_PORT:-8003}
      COMMUNICATION_API_URL: http://communication-api:${COMMUNICATION_API_PORT:-8004}
      PLATFORM_SERVICES_API_URL: http://platform-services-api:${PLATFORM_SERVICES_API_PORT:-8005}
      KB_API_URL: http://kb-api:${KB_API_PORT:-8006}
      LEGACY_API_URL: http://api:${API_PORT:-4500}
    ports:
      - "${B4F_API_PORT:-8000}:${B4F_API_PORT:-8000}"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${B4F_API_PORT:-8000}/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    volumes:
      - ./apis/shared:/app/shared:ro
    depends_on:
      auth-api:
        condition: service_healthy
      crm-backend-api:
        condition: service_healthy

  # ═══════════════════════════════════════
  # FRONTEND
  # ═══════════════════════════════════════
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: croo-frontend
    restart: unless-stopped
    ports:
      - "4700:4700"
    depends_on:
      b4f-api:
        condition: service_healthy

  # ═══════════════════════════════════════
  # AUTH API (microservice — Phase 1)
  # ═══════════════════════════════════════
  auth-api:
    build:
      context: ./apis/auth-api
      dockerfile: Dockerfile
    container_name: croo-auth-api
    restart: unless-stopped
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      DEBUG: ${DEBUG:-true}
      PORT: ${AUTH_API_PORT:-8001}
      DATABASE_URL: postgresql://${DB_USER:-croo}:${DB_PASSWORD:-croo}@database:5432/${DB_NAME:-croo_digital_experience}
      SECRET_KEY: ${SECRET_KEY}
      JWT_SECRET_KEY: ${SECRET_KEY}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-30}
      REFRESH_TOKEN_EXPIRE_DAYS: ${REFRESH_TOKEN_EXPIRE_DAYS:-7}
      CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost:4200","http://localhost:4511","http://localhost:4700"]}
      RATE_LIMIT_MAX_ATTEMPTS: ${RATE_LIMIT_MAX_ATTEMPTS:-5}
      RATE_LIMIT_WINDOW_MINUTES: ${RATE_LIMIT_WINDOW_MINUTES:-15}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@croo.digital}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      ADMIN_FIRST_NAME: ${ADMIN_FIRST_NAME:-Admin}
      ADMIN_LAST_NAME: ${ADMIN_LAST_NAME:-Croo}
    ports:
      - "${AUTH_API_PORT:-8001}:${AUTH_API_PORT:-8001}"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${AUTH_API_PORT:-8001}/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    volumes:
      - ./apis/shared:/app/shared:ro
    depends_on:
      database:
        condition: service_healthy

  # ═══════════════════════════════════════
  # CRM BACKEND API (microservice — Phase 2)
  # ═══════════════════════════════════════
  crm-backend-api:
    build:
      context: ./apis/crm-backend-api
      dockerfile: Dockerfile
    container_name: croo-crm-backend-api
    restart: unless-stopped
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      DEBUG: ${DEBUG:-true}
      PORT: ${CRM_API_PORT:-8002}
      DATABASE_URL: postgresql://${DB_USER:-croo}:${DB_PASSWORD:-croo}@database:5432/${DB_NAME:-croo_digital_experience}
      JWT_SECRET_KEY: ${SECRET_KEY}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost:4200","http://localhost:4511","http://localhost:4700"]}
    ports:
      - "${CRM_API_PORT:-8002}:${CRM_API_PORT:-8002}"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${CRM_API_PORT:-8002}/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    volumes:
      - ./apis/shared:/app/shared:ro
    depends_on:
      database:
        condition: service_healthy

  # ═══════════════════════════════════════
  # AI AGENT API (microservice — Phase 3)
  # ═══════════════════════════════════════
  ai-agent-api:
    build:
      context: ./apis/ai-agent-api
      dockerfile: Dockerfile
    container_name: croo-ai-agent-api
    restart: unless-stopped
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      DEBUG: ${DEBUG:-true}
      PORT: ${AI_AGENT_API_PORT:-8003}
      DATABASE_URL: postgresql://${DB_USER:-croo}:${DB_PASSWORD:-croo}@database:5432/${DB_NAME:-croo_digital_experience}
      JWT_SECRET_KEY: ${SECRET_KEY}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost:4200","http://localhost:4511","http://localhost:4700"]}
      GROQ_API_KEY: ${GROQ_API_KEY}
      DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY}
    ports:
      - "${AI_AGENT_API_PORT:-8003}:${AI_AGENT_API_PORT:-8003}"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${AI_AGENT_API_PORT:-8003}/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    volumes:
      - ./apis/shared:/app/shared:ro
    depends_on:
      database:
        condition: service_healthy

  # ═══════════════════════════════════════
  # COMMUNICATION API (microservice — Phase 4)
  # ═══════════════════════════════════════
  communication-api:
    build:
      context: ./apis/communication-api
      dockerfile: Dockerfile
    container_name: croo-communication-api
    restart: unless-stopped
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      DEBUG: ${DEBUG:-true}
      PORT: ${COMMUNICATION_API_PORT:-8004}
      DATABASE_URL: postgresql://${DB_USER:-croo}:${DB_PASSWORD:-croo}@database:5432/${DB_NAME:-croo_digital_experience}
      JWT_SECRET_KEY: ${SECRET_KEY}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost:4200","http://localhost:4511","http://localhost:4700"]}
      MS365_CLIENT_ID: ${MS365_CLIENT_ID}
      MS365_CLIENT_SECRET: ${MS365_CLIENT_SECRET}
      MS365_TENANT_ID: ${MS365_TENANT_ID}
      MS365_REDIRECT_URI: ${MS365_REDIRECT_URI}
    ports:
      - "${COMMUNICATION_API_PORT:-8004}:${COMMUNICATION_API_PORT:-8004}"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${COMMUNICATION_API_PORT:-8004}/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    volumes:
      - ./apis/shared:/app/shared:ro
    depends_on:
      database:
        condition: service_healthy

  # ═══════════════════════════════════════
  # PLATFORM SERVICES API (microservice — Phase 5)
  # ═══════════════════════════════════════
  platform-services-api:
    build:
      context: ./apis/platform-services-api
      dockerfile: Dockerfile
    container_name: croo-platform-services-api
    restart: unless-stopped
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      DEBUG: ${DEBUG:-true}
      PORT: ${PLATFORM_SERVICES_API_PORT:-8005}
      DATABASE_URL: postgresql://${DB_USER:-croo}:${DB_PASSWORD:-croo}@database:5432/${DB_NAME:-croo_digital_experience}
      JWT_SECRET_KEY: ${SECRET_KEY}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost:4200","http://localhost:4511","http://localhost:4700"]}
    ports:
      - "${PLATFORM_SERVICES_API_PORT:-8005}:${PLATFORM_SERVICES_API_PORT:-8005}"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${PLATFORM_SERVICES_API_PORT:-8005}/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    volumes:
      - ./apis/shared:/app/shared:ro
    depends_on:
      database:
        condition: service_healthy

  # ═══════════════════════════════════════
  # KNOWLEDGE BASE API (microservice — Phase 6)
  # ═══════════════════════════════════════
  kb-api:
    build:
      context: ./apis/kb-api
      dockerfile: Dockerfile
    container_name: croo-kb-api
    restart: unless-stopped
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      DEBUG: ${DEBUG:-true}
      PORT: ${KB_API_PORT:-8006}
      DATABASE_URL: postgresql://${DB_USER:-croo}:${DB_PASSWORD:-croo}@database:5432/${DB_NAME:-croo_digital_experience}
      JWT_SECRET_KEY: ${SECRET_KEY}
      JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
      CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost:4200","http://localhost:4511","http://localhost:4700"]}
    ports:
      - "${KB_API_PORT:-8006}:${KB_API_PORT:-8006}"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:${KB_API_PORT:-8006}/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    volumes:
      - ./apis/shared:/app/shared:ro
    depends_on:
      database:
        condition: service_healthy

volumes:
  pgdata:
