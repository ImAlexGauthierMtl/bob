x-common-env: &common-env
  ENVIRONMENT: ${ENVIRONMENT:-development}
  DEBUG: ${DEBUG:-true}
  JWT_SECRET_KEY: ${SECRET_KEY}
  JWT_ALGORITHM: ${JWT_ALGORITHM:-HS256}
  CORS_ORIGINS: ${CORS_ORIGINS:-["http://localhost:4200","http://localhost:4700","http://localhost:24200","http://localhost:4300"]}
  INGRESS_URL: ${INGRESS_URL:-http://localhost:4200}
  REDIS_URL: redis://redis:6379/0
  EVENT_BUS_BACKEND: ${EVENT_BUS_BACKEND:-redis}
  OTLP_URL: ${OTLP_URL:-}

x-db-env: &db-env
  DATABASE_URL: postgresql://${DB_USER:-croo}:${DB_PASSWORD:-croo}@database:5432/${DB_NAME:-croo_digital_experience}

x-backend-urls: &backend-urls
  USER_BACKEND_API_URL: http://user-backend-api:9001
  CONTACT_BACKEND_API_URL: http://contact-backend-api:9002
  ORG_BACKEND_API_URL: http://org-backend-api:9003
  OPPORTUNITY_BACKEND_API_URL: http://opportunity-backend-api:9004
  ACTIVITY_BACKEND_API_URL: http://activity-backend-api:9005
  PRODUCT_BACKEND_API_URL: http://product-backend-api:9006
  EMAIL_BACKEND_API_URL: http://email-backend-api:9007
  AGENT_BACKEND_API_URL: http://agent-backend-api:9008
  WORKFLOW_BACKEND_API_URL: http://workflow-backend-api:9009
  KB_BACKEND_API_URL: http://kb-backend-api:9010
  USAGE_BACKEND_API_URL: http://usage-backend-api:9011

x-shared-volume: &shared-volume
  - ./apis/shared:/shared:ro

x-healthcheck: &healthcheck
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 15s

services:
  # ═══════════════════════════════════════
  # INFRASTRUCTURE
  # ═══════════════════════════════════════
  database:
    image: postgres:16-alpine
    container_name: croo-database
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-croo}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-croo}
      POSTGRES_DB: ${DB_NAME:-croo_digital_experience}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-croo} -d ${DB_NAME:-croo_digital_experience}"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: croo-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6380}:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ═══════════════════════════════════════
  # B4F LAYER — Business logic (exposed)
  # ═══════════════════════════════════════
  auth-b4f-api:
    build:
      context: .
      dockerfile: apis/exposed/auth-b4f-api/Dockerfile
      target: dev
    container_name: croo-auth-b4f-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *backend-urls]
      PORT: 8001
      SECRET_KEY: ${SECRET_KEY}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-30}
      REFRESH_TOKEN_EXPIRE_DAYS: ${REFRESH_TOKEN_EXPIRE_DAYS:-7}
      RATE_LIMIT_MAX_ATTEMPTS: ${RATE_LIMIT_MAX_ATTEMPTS:-5}
      RATE_LIMIT_WINDOW_MINUTES: ${RATE_LIMIT_WINDOW_MINUTES:-15}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@croo.digital}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
    ports:
      - "${AUTH_B4F_PORT:-8001}:8001"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8001/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/exposed/auth-b4f-api/app:/app/app
      - ./apis/exposed/auth-b4f-api/main.py:/app/main.py

    depends_on:
      user-backend-api:
        condition: service_healthy

  crm-b4f-api:
    build:
      context: .
      dockerfile: apis/exposed/crm-b4f-api/Dockerfile
      target: dev
    container_name: croo-crm-b4f-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *backend-urls]
      PORT: 8002
    ports:
      - "${CRM_B4F_PORT:-8002}:8002"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8002/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/exposed/crm-b4f-api/app:/app/app
      - ./apis/exposed/crm-b4f-api/main.py:/app/main.py

    depends_on:
      contact-backend-api:
        condition: service_healthy
      org-backend-api:
        condition: service_healthy

  ai-agent-b4f-api:
    build:
      context: .
      dockerfile: apis/exposed/ai-agent-b4f-api/Dockerfile
      target: dev
    container_name: croo-ai-agent-b4f-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *backend-urls]
      PORT: 8003
      GROQ_API_KEY: ${GROQ_API_KEY}
      DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY}
    ports:
      - "${AI_AGENT_B4F_PORT:-8003}:8003"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8003/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/exposed/ai-agent-b4f-api/app:/app/app
      - ./apis/exposed/ai-agent-b4f-api/main.py:/app/main.py

    depends_on:
      agent-backend-api:
        condition: service_healthy

  communication-b4f-api:
    build:
      context: .
      dockerfile: apis/exposed/communication-b4f-api/Dockerfile
      target: dev
    container_name: croo-communication-b4f-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *backend-urls]
      PORT: 8004
      MS365_CLIENT_ID: ${MS365_CLIENT_ID}
      MS365_CLIENT_SECRET: ${MS365_CLIENT_SECRET}
      MS365_TENANT_ID: ${MS365_TENANT_ID}
      MS365_REDIRECT_URI: ${MS365_REDIRECT_URI}
      MS365_WEBHOOK_HOST: ${MS365_WEBHOOK_HOST}
      WEBHOOK_API_KEY: ${WEBHOOK_API_KEY}
      MEMBRANE_WORKSPACE_KEY: ${MEMBRANE_WORKSPACE_KEY}
      MEMBRANE_WORKSPACE_SECRET: ${MEMBRANE_WORKSPACE_SECRET}
      MEMBRANE_CLIENT_TOKEN: ${MEMBRANE_CLIENT_TOKEN}
      MEMBRANE_API_URL: ${MEMBRANE_API_URL}
      MEMBRANE_WEBHOOK_SECRET: ${MEMBRANE_WEBHOOK_SECRET}
      PUBLIC_API_URL: ${PUBLIC_API_URL:-http://localhost:28004}
    ports:
      - "${COMMUNICATION_B4F_PORT:-8004}:8004"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8004/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/exposed/communication-b4f-api/app:/app/app
      - ./apis/exposed/communication-b4f-api/main.py:/app/main.py

    depends_on:
      email-backend-api:
        condition: service_healthy

  platform-b4f-api:
    build:
      context: .
      dockerfile: apis/exposed/platform-b4f-api/Dockerfile
      target: dev
    container_name: croo-platform-b4f-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *backend-urls]
      PORT: 8005
    ports:
      - "${PLATFORM_B4F_PORT:-8005}:8005"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8005/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/exposed/platform-b4f-api/app:/app/app
      - ./apis/exposed/platform-b4f-api/main.py:/app/main.py

    depends_on:
      workflow-backend-api:
        condition: service_healthy
      usage-backend-api:
        condition: service_healthy

  kb-b4f-api:
    build:
      context: .
      dockerfile: apis/exposed/kb-b4f-api/Dockerfile
      target: dev
    container_name: croo-kb-b4f-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *backend-urls]
      PORT: 8006
    ports:
      - "${KB_B4F_PORT:-8006}:8006"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8006/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/exposed/kb-b4f-api/app:/app/app
      - ./apis/exposed/kb-b4f-api/main.py:/app/main.py

    depends_on:
      kb-backend-api:
        condition: service_healthy

  # ═══════════════════════════════════════
  # BACKEND LAYER — CRUD + Events (internal)
  # ═══════════════════════════════════════
  user-backend-api:
    build:
      context: .
      dockerfile: apis/internal/user-backend-api/Dockerfile
      target: dev
    container_name: croo-user-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9001
    ports:
      - "${USER_BACKEND_PORT:-9101}:9001"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9001/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/user-backend-api/app:/app/app
      - ./apis/internal/user-backend-api/main.py:/app/main.py
      - ./apis/internal/user-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  contact-backend-api:
    build:
      context: .
      dockerfile: apis/internal/contact-backend-api/Dockerfile
      target: dev
    container_name: croo-contact-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9002
    ports:
      - "${CONTACT_BACKEND_PORT:-9002}:9002"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9002/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/contact-backend-api/app:/app/app
      - ./apis/internal/contact-backend-api/main.py:/app/main.py
      - ./apis/internal/contact-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  org-backend-api:
    build:
      context: .
      dockerfile: apis/internal/org-backend-api/Dockerfile
      target: dev
    container_name: croo-org-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9003
    ports:
      - "${ORG_BACKEND_PORT:-9003}:9003"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9003/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/org-backend-api/app:/app/app
      - ./apis/internal/org-backend-api/main.py:/app/main.py
      - ./apis/internal/org-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  opportunity-backend-api:
    build:
      context: .
      dockerfile: apis/internal/opportunity-backend-api/Dockerfile
      target: dev
    container_name: croo-opportunity-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9004
    ports:
      - "${OPPORTUNITY_BACKEND_PORT:-9004}:9004"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9004/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/opportunity-backend-api/app:/app/app
      - ./apis/internal/opportunity-backend-api/main.py:/app/main.py
      - ./apis/internal/opportunity-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  activity-backend-api:
    build:
      context: .
      dockerfile: apis/internal/activity-backend-api/Dockerfile
      target: dev
    container_name: croo-activity-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9005
    ports:
      - "${ACTIVITY_BACKEND_PORT:-9005}:9005"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9005/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/activity-backend-api/app:/app/app
      - ./apis/internal/activity-backend-api/main.py:/app/main.py
      - ./apis/internal/activity-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  product-backend-api:
    build:
      context: .
      dockerfile: apis/internal/product-backend-api/Dockerfile
      target: dev
    container_name: croo-product-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9006
    ports:
      - "${PRODUCT_BACKEND_PORT:-9006}:9006"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9006/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/product-backend-api/app:/app/app
      - ./apis/internal/product-backend-api/main.py:/app/main.py
      - ./apis/internal/product-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  email-backend-api:
    build:
      context: .
      dockerfile: apis/internal/email-backend-api/Dockerfile
      target: dev
    container_name: croo-email-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9007
    ports:
      - "${EMAIL_BACKEND_PORT:-9007}:9007"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9007/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/email-backend-api/app:/app/app
      - ./apis/internal/email-backend-api/main.py:/app/main.py
      - ./apis/internal/email-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  agent-backend-api:
    build:
      context: .
      dockerfile: apis/internal/agent-backend-api/Dockerfile
      target: dev
    container_name: croo-agent-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9008
    ports:
      - "${AGENT_BACKEND_PORT:-9008}:9008"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9008/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/agent-backend-api/app:/app/app
      - ./apis/internal/agent-backend-api/main.py:/app/main.py
      - ./apis/internal/agent-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  workflow-backend-api:
    build:
      context: .
      dockerfile: apis/internal/workflow-backend-api/Dockerfile
      target: dev
    container_name: croo-workflow-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9009
    ports:
      - "${WORKFLOW_BACKEND_PORT:-9009}:9009"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9009/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/workflow-backend-api/app:/app/app
      - ./apis/internal/workflow-backend-api/main.py:/app/main.py
      - ./apis/internal/workflow-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  kb-backend-api:
    build:
      context: .
      dockerfile: apis/internal/kb-backend-api/Dockerfile
      target: dev
    container_name: croo-kb-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9010
    ports:
      - "${KB_BACKEND_PORT:-9010}:9010"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9010/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/kb-backend-api/app:/app/app
      - ./apis/internal/kb-backend-api/main.py:/app/main.py
      - ./apis/internal/kb-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  usage-backend-api:
    build:
      context: .
      dockerfile: apis/internal/usage-backend-api/Dockerfile
      target: dev
    container_name: croo-usage-backend-api
    restart: unless-stopped
    environment:
      <<: [*common-env, *db-env]
      PORT: 9011
    ports:
      - "${USAGE_BACKEND_PORT:-9011}:9011"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:9011/health || exit 1"]
      <<: *healthcheck
    volumes:
      - ./apis/internal/usage-backend-api/app:/app/app
      - ./apis/internal/usage-backend-api/main.py:/app/main.py
      - ./apis/internal/usage-backend-api/alembic:/app/alembic

    depends_on:
      database:
        condition: service_healthy
      redis:
        condition: service_healthy

  # ═══════════════════════════════════════
  # FRONTEND
  # ═══════════════════════════════════════
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      target: dev
    container_name: croo-frontend
    restart: unless-stopped
    environment:
      NODE_OPTIONS: "--max-old-space-size=768"
    deploy:
      resources:
        limits:
          memory: 3g
    ports:
      - "${FRONTEND_PORT:-4200}:4200"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      auth-b4f-api:
        condition: service_healthy

volumes:
  pgdata:
