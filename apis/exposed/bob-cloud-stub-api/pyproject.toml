[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bob-cloud-stub-api"
version = "0.1.0"
description = "Local/CI Bob Cloud contract stub for Croo Digital Experience"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "pydantic>=2.0",
  "pydantic-settings>=2.0",
  "structlog>=23.0",
  "httpx>=0.24",
  "python-jose[cryptography]>=3.3",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "pytest-cov>=5.0",
  "import-linter>=2.0",
]

[tool.setuptools]
packages = ["bob_cloud_stub_api"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "../.."]
asyncio_mode = "auto"

[tool.importlinter]
root_package = "app"
include_external_packages = true

[[tool.importlinter.contracts]]
name = "domain application infrastructure do not import presentation"
type = "forbidden"
source_modules = [
  "app.domain",
  "app.application",
  "app.infrastructure",
]
forbidden_modules = ["app.presentation"]

[[tool.importlinter.contracts]]
name = "application avoids framework dependencies"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["app.application"]
forbidden_modules = [
  "fastapi",
  "sqlalchemy",
  "httpx",
  "redis",
  "pydantic_settings",
]

[[tool.importlinter.contracts]]
name = "domain avoids framework dependencies"
type = "forbidden"
allow_indirect_imports = true
source_modules = ["app.domain"]
forbidden_modules = [
  "fastapi",
  "sqlalchemy",
  "pydantic",
  "pydantic_settings",
  "httpx",
  "redis",
]
