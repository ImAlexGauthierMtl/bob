# Shared library dependencies
# Install in each API: pip install -e ../shared OR via PYTHONPATH
pydantic>=2.0
pydantic-settings>=2.0
sqlalchemy>=2.0
structlog>=23.0
python-jose[cryptography]>=3.3
httpx>=0.24
